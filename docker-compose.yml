services:
  battery-service:
    build: .
    ports:
      - "3000:3000"
    environment:
      API_KEY: demo-api-key
      DATABASE_PATH: /data/battery.sqlite
    volumes:
      - ./data:/data
    command: node dist/index.js
